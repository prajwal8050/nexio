spring.application.name=nexio
server.port=9010

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/nexio_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=8050@Pra
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Enable H2 as a fallback if MySQL fails (optional, but requested MySQL)
# To use H2, comment out MySQL and uncomment the H2 lines from previous version

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=prajwalgowd88@gmail.com
spring.mail.password=kcqy zuma hvqm jygn
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
